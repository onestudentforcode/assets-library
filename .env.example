DATABASE_PATH=./data/assets.db
MEDIA_ROOT=./media
MAX_IMAGE_BYTES=20971520
MAX_VIDEO_BYTES=209715200

# Independent Python Scene Detection Service. Its container owns its own
# Python runtime and FFmpeg installation.
SCENE_DETECT_BASE_URL=http://127.0.0.1:28200
SCENE_DETECT_TIMEOUT_MS=600000

# openai_chat_completions or openai_responses
MODEL_PROTOCOL=openai_chat_completions
# NewAPI/OpenAI-compatible Base URL should include /v1.
MODEL_BASE_URL=
# Leave empty only when the gateway has authentication disabled.
MODEL_API_KEY=
MODEL_NAME=
MODEL_TIMEOUT_MS=120000
MODEL_VIDEO_TIMEOUT_MS=300000
MODEL_RETRY_COUNT=1
# Qwen3.7 defaults to thinking mode; disable it for fast structured extraction.
MODEL_ENABLE_THINKING=false

# frames: analyze browser-generated JPEG keyframes; disabled: reject video analysis.
MODEL_VIDEO_MODE=frames

# Semantic search: local Chroma persists analysis-result vectors.
CHROMA_URL=http://127.0.0.1:8000
CHROMA_COLLECTION=asset_analysis
CHROMA_TENANT=default_tenant
CHROMA_DATABASE=default_database
# OpenAI-compatible embedding endpoint. Leave EMBEDDING_BASE_URL empty to reuse MODEL_BASE_URL.
EMBEDDING_BASE_URL=
EMBEDDING_API_KEY=
EMBEDDING_MODEL=
